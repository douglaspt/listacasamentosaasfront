/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Presente} from './presente';

describe('Presente', () => {
  it('should create an instance', () => {
    expect(new Presente()).toBeTruthy();
  });
});
